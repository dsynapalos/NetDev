---
- hosts: "{{ CSR1000V_HOST }}"
  become: yes
  become_method: enable
  connection: network_cli
  
  tasks:
    - name: run multiple commands on remote nodes
      ios_command:
        commands:
          - show runn
