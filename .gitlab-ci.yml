image: gibby/openconnect 
#ubuntu:18.04

stages:
  - connect

before_script:
  - apt-get update -y
  - apt-get upgrade -y
  - apt-get install -y python3 python3-pip openconnect
  - pip3 install ansible
  - ansible --version
  



connect:
  stage: connect
  script:
    - openconnect --help
